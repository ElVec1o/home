---
import '../styles/global.css';
const { title } = Astro.props;
const base = import.meta.env.BASE_URL;
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>
</head>
<body>
  <nav>
    <a href={`${base}/`} class="nav-home">
      <img src={`${base}/img/logo-elvec1o.png`} alt="elvec1o" class="nav-logo" />
      <span class="version">v0.223</span>
    </a>
    <a href={`${base}/posts/`}>Posts</a>
    <a href={`${base}/posts/writeups/`}>Writeups</a>
    <a href={`${base}/posts/breaks/`}>Breaks</a>
    <a href={`${base}/files/`}>Files</a>
    <a href={`${base}/books/`}>Books</a>
    <a href="https://x.com/elvec10" target="_blank">X</a>
  </nav>
  <slot />
  <footer>
    elvec1o — 2026
  </footer>
</body>
</html>
